# https://www.acmicpc.net/problem/11047

# 간단히 생각해서 풀 수 있는 문제
# 큰 값부터 하나씩 나누고 몫과 나머지를 필요에 맞게 사용

# 코드
num, won = map(int,input().split())
values=[]


for i in range(num):
    value=int(input())
    values.append(value)
    
values.reverse()
cnt=0
for value in values:
    q,won=divmod(won,value)
    if q>0:
        cnt+=q
    if won==0:
        break
print(cnt)
